import React from 'react';

const NullLenghtTasks = () => (
    <div
        id="nullTask"
        style={{ margin: '10px', opacity: 0.6 }}
    >You don't have a do-to at the moment!
    </div>
);

export default NullLenghtTasks;
